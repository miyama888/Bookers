<header>
	<h1 class = "headline">Books</h1>
	<p id ="notice"></p>
</header>
<body>
<table>
	<thead>
		<tr>
			<th>Title</th>
			<th>Body</th>
			<th colspan="3"></th>
		</tr>
	</thead>

  <tdody>
    <% @books.each do |t|%>
	     <tr>
        <td><%= post.title %></td>
        <td><%= post.body %></td>
        <td><%= link_to "show", book_path(book.id) %>
            <%= link_to "edit", edit_book_path(book.id) %>
            <%= link_to "destroy", destroy_book_path(book.id), method: :delete, "data-confirm" => "Are you sure?" %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<h2>New book</h2>
<%= form_for(@books) do |t| %>


  
    <label>Title</label>
    <%= f.text_field :title %>

  
    <label>Body</label>
   <%= f.text_area :body %>

   <br><%= f.submit 'Create Book' %></br>
   <% end %>

  <div class="actions">
    <input type="submit" name="commit" value="Create Book" data-disable-with="Create Book">
  </div>
	</form>
</body>