<h1>Editing Book</h1>
<%= form_for(@book) do |f| %>
	<strong>Title:</strong>
	<%= f.text_field :title %>
	<strong>Body:</strong>
	<%= f.text_area :body %>

	 <br><%= f.submit 'Update Book' %></br>
<% end %>

<%= link_to "Show", book_path(@book) %>
<%= link_to "Back", books_path(@book) %>